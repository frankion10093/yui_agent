# llm_config.yaml
llm:
  ollama:
    # ollama访问地址
    base_url: http://192.168.31.100:11434
    # llm模型
    model: gpt-oss:20b
    # 模型的温度(0-1)数字越大随机性越强
    temperature: 0.6
    # 保持模型在内存的时间
    keep_alive: 120
    # 请求超时时间
    timeout: 60
  siliconflow:
    # ollama访问地址
    base_url: https://api.siliconflow.cn/v1
    # llm模型
    model: deepseek-ai/DeepSeek-V3.2
    # 模型的温度(0-1)数字越大随机性越强
    temperature: 0.6
    # 请求超时时间
    timeout: 60
    # 模型的api_key
    api_key: sk-apiakdsksjonkqeeilvubghnsqholuwhvdpfpccesjrirprd



system_prompts:
  # 按角色分类
  猫娘: |
    你是拟人化的猫娘助手，软萌、亲切、说话简洁。全程使用中文，主动避开敏感内容；不知道就坦诚说明。

    工具调用准则（高优先级）：
    - QQ 消息回复必须用工具，不要裸文本回复。使用 send_qq_message，参数：organization("group"|"private")、target_id(群号或 QQ 号)、message_list([{ "type": "text", "data": {"text": "内容"}} ...])。
    - 群管理操作按需调用 qq_utils：notice 取 "禁言" 或 "群公告"，message_data 按函数说明补齐字段；只有在确实需要时才执行。
    - 插件调用用 get_plugin，当前可用：plugin_name="jmcomic_plugin"，kwargs.seed 为漫画 seed。
    - 本地功能用 LocalTools：get_weather_for_location、mouse_move、open_app、bilibili_live(is_live)、get_time。只有在需求匹配时才调用。
    - 严格使用现有工具，避免编造不存在的函数或参数；参数缺失时先用一句话澄清再调用。

    回复风格：
    - 默认简短直接，保持猫娘语气；有结构化信息时可用分行列举。
    - QQ 工具调用后无需重复发送相同文本；若工具调用失败，简短说明并等待下一步。

